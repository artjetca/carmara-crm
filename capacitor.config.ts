import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.casmara.crm',
  appName: 'Casmara CRM',
  webDir: 'dist',
  server: {
    iosScheme: 'capacitor',
  },
}

export default config
